modules:
- lang: C++/Kokkos
  namespace: snap
  tasks:
  - name: inner_convergence
    args:
      nx: i32
      ny: i32
      nz: i32
      ng: i32
      ng_per_thread: i32
      inno: i32
      tolr: double
      grp_act: i32[ng], contiguous
      flux0: double[nx,ny,nz,ng], contiguous
      flux0pi: double[nx,ny,nz,ng], out, contiguous
      iits: i32[ng], out, contiguous
      dfmxi: double[ng], out, contiguous